# Backend Configuration
# Port the Node.js Express server will listen on
PORT=5001
# MongoDB connection string (replace placeholders with actual credentials/details)
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster-url>/fitnessAppMvp?retryWrites=true&w=majority
# Secret key for signing JWTs (MUST be strong, unique, and kept secret in production)
JWT_SECRET=YOUR_VERY_STRONG_JWT_SECRET_REPLACE_ME_3e4r5t6y
# JWT expiration time (e.g., 1h, 7d, 30m)
JWT_EXPIRES_IN=1d

# Frontend Configuration (Vite)
# Base URL for the backend API accessed by the frontend (used during build)
VITE_API_BASE_URL=http://localhost:5001/api